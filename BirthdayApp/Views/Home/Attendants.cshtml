@model IEnumerable<BirthdayApp.Models.InvitationModel>

@if(Model.Count() == 1)
{
    <p>You are the first one here!</p>
}
else
{
    <hr />
    <p>
        @foreach(var i in Model)
        {
            <label class="badge badge-success">@i.Name</label>
        }
    </p>
}
